{
  "name": "jnoria-redis-lru-cache",
  "version": "1.0.0",
  "description": "LRU (Least Recently Used) cache with time expiration using redis",
  "main": "dist/lru-cache.js",
  "types": "dist/lru-cache.d.ts",
  "files": [
    "/dist"
  ],
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest --runInBand --detectOpenHandles --forceExit",
    "coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "Juan Noria",
  "license": "ISC",
  "dependencies": {
    "redis": "^4.6.10"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.7.1",
    "jest": "^29.7.0",
    "redis": "^4.6.10",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
